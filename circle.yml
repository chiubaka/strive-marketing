machine:
  node:
    version: 8.5.0
  npm:
    version: 5.4.2
  hosts:
    strive.do: 138.68.233.83
  environment:
    CIRCLE_ARTIFACTS: ~/strive-marketing/dist

dependencies:
  override:
    - yarn install

test:
  override:
    - yarn build

deployment:
  staging:
    branch: master
    commands:
      - scp circleci@strive.do:/var/www/strive/ dist/*
